[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "mahou-libs"
version = "0.1.0"
description = "Utilities shared across my Python projects"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["mahou_libs"]

